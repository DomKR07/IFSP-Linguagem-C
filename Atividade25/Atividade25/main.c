#include <stdio.h>
#include "Calculadora.h"

int main() {
    Calculadora *calc = criarCalculadora();
    int escolha;
    float valor;

    do {

    } while (escolha != 6);

    destruirCalculadora(calc);

    return 0;
}
